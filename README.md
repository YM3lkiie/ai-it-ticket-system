# AI IT Ticket System
Personal project to build an AI-powered IT support ticket platform.

# Database Setup
Run the following command to initialize the database:
=> mysql -u root -p < database/init.sql
